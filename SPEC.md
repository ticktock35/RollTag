# RollTag 規格書

狀態：與程式同步的活文件。改行為時必須一起改這份。  
產品版本：第一版（1.0）  
文件日期：2026-09-22（點格線立刻反白，播放器不擋選取）

獨立工具。在自己的硬碟倉庫裡分析 B-roll、打標、切段、搜尋與預覽。搜尋綁倉庫、不綁單一後製專案：DaVinci 每個案子都要重新索引，除非維護超大 Super Bin。這一版不做 DaVinci 串接。

---

## 1. 產品範圍

### 1.1 這一版做

- 登記本機 footage warehouse（外接硬碟或資料夾）
- 快速認檔、離線／在線、改名／缺失／內容變更
- 預覽、格線 hover 播放、單支 trim（照片不播放、不 trim；音訊可聽）。Trim 要先按「剪輯」才開獨立視窗。影片／音訊播放器可拉時間軸跳轉
- 預設階層 tags + 使用者自訂 tags（單支與批次）。畫面語言是英文時，預設分類與細項顯示英文名；自訂標仍是使用者打的原文
- 重複檔找出與處理（選留下哪一支後，其他複本丟進垃圾桶）
- 關鍵字／標籤搜尋（hybrid；只查在線且找得到的檔）。索引在倉庫裡，換 DaVinci 專案不必重做。左側可展開倉庫下的目錄樹，點一層或勾選幾個重點目錄，搜尋、打標與重複檔只先做那些資料夾及其下層
- 加入大目錄時顯示進度（檔名、數量、百分比）
- Preference 可設定 AI 平台、API key、該平台能力足夠的模型、檔頭時間是否略過不像拍攝日的值、常用字詞對應表，以及播放／媒體庫／重複檔各情境的快捷鍵
- 手動 AI 打標（有 key 且已選可打的影片或照片才出現；已打過也可再打。檢查器／⌥⌘T 打完要確認。側欄「未打標」與媒體區右鍵可批次打、不必確認。音訊、太小或解不開的批次會跳過）

### 1.2 資料模型預留、這一版不做邏輯

- 整倉搬家（metadata 必須跟著走；路徑已存相對路徑）
- CLIP 畫面相似
- 倉庫丟進 NAS、DaVinci 串接

### 1.3 這一版不做

- DaVinci 串接
- 假設已有 NAS
- Linux／Windows 客戶端（之後另做，不改掉 Mac 原生 UI）
- 時間軸、調色、多軌
- 公開分享
- 匯入即自動 AI 打標

---

## 2. 平台與操作

- 主要環境：macOS 14+。第一版只做 Mac App。關閉最後一個視窗即結束程式（⌘Q 也會）。
- UI：Swift + SwiftUI。預覽、hover 播放、trim 走 AVFoundation。
- 相似搜尋與 AI 打標：本機 sidecar（Python），不做介面。抽幀在 App 內做。
- 操作與外觀比照 Mac：選取、拖放、快捷鍵、視窗、選單。
- 介面語言：繁中為主，附完整英文 localization。跟著系統語言切換。
- 不走 App Store sandbox；仍存 security-scoped bookmark。
- 支援副檔名：
  - 影片：`mov`、`mp4`、`m4v`、`avi`、`mkv`、`mxf`
  - 照片（含 iPhone）：`heic`、`heif`、`jpg`、`jpeg`、`png`、`webp`、`gif`
  - 音訊：`mp3`、`m4a`、`aac`、`wav`
- 照片顯示靜態預覽。音訊顯示喇叭、可聽。影片在上半左側播放器播，可拉時間軸；格線格子仍可 hover 預覽。
- 掃描不抽影片／照片預覽，避免一次解上千支 4K 把整台 Mac 卡住。掃描讀長度、EXIF／QuickTime、DJI 檔名時間與 GPS（有才寫），不抽幀。
- 縮圖在格子或重複檔比較真正要出現時才做，全程式同時只解一支；做好寫進 `.rolltag/thumbs/`（約 480px）。從原檔做縮圖時依 EXIF／TIFF 方向轉正（iPhone 直立照常把像素存成橫的）。還沒有縮圖就當場從原檔做並顯示。已存縮圖若比例與轉正後的原檔對不上就重做。原檔不在了（掃描標 missing、刪列、或比較時發現檔案沒了）就刪掉對應 thumbs，不留孤兒。格線只把畫面上的格子解成約 320px，不把整庫縮圖留在記憶體。點選立刻反白與換資料欄；大圖先用已存縮圖，再從原檔抽較大張，不擋點選、不覆蓋格子縮圖。影片／音訊播放器在下一拍才換檔，避免 4K 檔卡住格線。不做系統 Quick Look 批次。
- 照片與影片的格線、重複檔比較都依轉正後的實際比例完整放入（約 9:16 到 21:9），不裁切、不強迫 16:9。手機直立拍的影片用軌道 transform 轉正。沒有尺寸時影片才退回 16:9。點選後上半左側可看更大張。資料庫裡的寬高是轉正後的。
- 有些影片系統與 AVFoundation 都解不開（常見：AVI／MKV、部分 DJI HEVC／10-bit／HDR、檔案未寫完或只有幾百 byte 的空檔）。仍可手打標；AI 打標會跳過太小或抽不出幀的檔，狀態列寫跳過幾支。

快捷鍵（預設；可在設定「快捷鍵」分頁改播放、媒體庫、重複檔的按鍵。選單 ⌘O／⌘R／⌘⇧D／⌘A／⌥⌘T／⌘,／⌘/ 不改）：

| 動作 | 預設快捷鍵 |
|---|---|
| 新增倉庫 | ⌘O |
| 重掃在線倉庫 | ⌘R |
| 重複檔視窗 | ⌘⇧D |
| 全選目前結果 | ⌘A |
| AI 打標目前選取 | ⌥⌘T |
| 確認 AI 打標已看完 | Enter（焦點不在輸入欄時；若正在確認丟重複檔，Enter 仍是丟垃圾桶） |
| 取消這次 AI 打標 | Esc（視窗模式、焦點不在輸入欄時；會拿掉這次打上的標。全螢幕時 Esc 仍是先離開全螢幕） |
| 格線移到相鄰素材 | ← ↑ → ↓ 或 W A S D（焦點在媒體庫格線時；⇧ 加選範圍） |
| 系統設定 | ⌘, |
| 播放／暫停目前選取的影片或音訊 | 空白鍵（焦點不在搜尋、備註或輸入欄時） |
| 進入或離開全螢幕 | P（焦點不在輸入欄時；重複檔對選中的那一支，再按 P 或 Esc 回來） |
| 全螢幕上一則／下一則 | [ 或 ,（含 ⇧ 的 <）／ ] 或 .（含 ⇧ 的 >）。依目前清單順序，找不到的檔會跳過；到頭或到尾就停。影片會接著播。重複檔全螢幕時在該組複本之間切 |
| 離開全螢幕；若已在視窗模式則取消選取 | Esc |
| 重複檔：留下最左／最右／全部 | A／D／S（焦點不在輸入欄時；看實體鍵，不看輸入法產出的字） |
| 重複檔：確認把其他複本丟進垃圾桶 | Enter（先按 A／D 或點「留下這一個」後） |
| 打開快捷鍵一覽 | ⌘/；也可從說明選單或檢視選單打開 |

字母快捷鍵看鍵盤上的實體鍵，不看輸入法打出的字。若正在用注音、拼音等輸入法，或按鍵變成ㄅ、あ這類字元，畫面會提示先切到 ABC 或美式鍵盤。焦點在搜尋、備註等輸入欄時不提示、也不觸發這些快捷鍵。同一情境內不能兩動作共用同一組鍵；Command 組合留給選單。設定裡點一組鍵再按下新的按鍵即可改；Esc 取消錄製。

---

## 3. 兩個資料位置

### 3.1 Preference（程式設定）

路徑：`~/rolltag/config.json`

只記：

- 倉庫名單：`id`、`name`、`path`、bookmark
- AI：使用中的平台、各平台 API key、從能力足夠清單選出的模型、是否略過不像拍攝日的檔頭時間（預設略過）、最近幾則「AI 打了什麼／使用者留下什麼」範例（不含畫面、不含各檔完整 tags）
- 快捷鍵：播放／媒體庫／重複檔各動作相對預設的按鍵覆寫（沒寫就用預設）
- 字詞對應表：使用者常用詞與其對應英文（沒寫就沒有）
- 線上／離線不寫進檔，啟動與磁碟掛載時用路徑是否存在計算

不把各檔的 tags 或搜尋索引寫進 preference。  
API key 是本機設定，不要把 `config.json` 拿去分享或進公開 git。  
手動 AI 打標時，幀、當下有的檔案脈絡（含路徑、時間、GPS），以及最近的打標範例會送到所選的 Gemini／OpenAI。

支援的 AI 平台（手動打標時，RollTag 抽 4–8 幀、把預設＋已有自訂 tag 選項給 agent，並附上當下有的檔案脈絡；預設分類只能選清單裡的 id，不能發明 id。另回 Getty／Pond5 英文關鍵字，以及看得清楚的短中文自訂詞）：

| 平台 | 這一版打標 | 可選模型（第一個為預設） |
|---|---|---|
| Google Gemini | 抽幀後送（最多 3 幀）；免費額度較緊，失敗立刻備援 OpenAI | `gemini-3.5-flash-lite`、`gemini-3.1-flash-lite`、`gemini-3.5-flash`、`gemini-3.8-flash` |
| OpenAI | 抽幀後送；Gemini 沒 key 或失敗時次之 | `gpt-4.1-mini`、`gpt-4.1`、`gpt-4o` |
| Twelve Labs | 可存 key 與模型，尚未接打標 | `pegasus-1.2` |
| 阿里雲百鍊／通義千問（DashScope） | 可存 key 與模型，尚未接打標 | `qwen-vl-max`、`qwen-vl-plus` |
| Anthropic Claude | 可存 key 與模型，尚未接打標 | `claude-sonnet-4-5`、`claude-sonnet-4`、`claude-opus-4` |

使用中的平台若是 Gemini／OpenAI 且有 key，先用它（含該平台選的模型），另一個有 key 當備援。沒選平台時依 Gemini → OpenAI。沒選過模型就用該平台清單第一個。  
已停用或不對新帳號開放的 Gemini id（例如 `gemini-2.5-flash-lite`）會自動改送到對應的 3.x 模型。  
匯入不自動跑 AI。填了 Gemini 或 OpenAI key **並且選了可打的影片或照片之後**（已打過標也算），右側與「標籤」選單才出現 AI 打標。只選音訊不出現。側欄「未打標」與媒體區右鍵另有「AI 批次打標」，不必確認。畫面／模型打上的 tag `source` 為 `ai`；按 AI 成功時由路徑套上的標 `source` 為 `path`。key 怎麼取得、以及 AI 標可能錯，見第 8 節。

倉庫路徑可在程式裡新增、編輯、刪除，也可把資料夾拖進視窗。  
刪的是「這個目錄還算不算倉庫」，不是硬碟上的片子。  
離線倉庫仍留在 preference。硬碟出現後重讀裡面的資料。

### 3.2 每個 warehouse 目錄裡的資料庫

路徑：`<倉庫>/.rolltag/`

- `warehouse.sqlite`：每段 footage 的相對路徑、身份、tags、切出來的片段、重複群組、拍攝時間／來源與 GPS（有才寫，在 `metadata_json`）
- `thumbs/{id}.jpg`：預覽縮圖
- `trimmed/`：單支 trim 匯出的新檔

跟著這顆硬碟走：換電腦、之後整批搬 NAS，metadata 還在倉庫裡面。  
路徑一律存相對倉庫根目錄。

SQLite 預留欄（第一版不寫入邏輯）：`embedding_id`、`ai_tags_json`、`moved_from`、`davinci_id`。  
Trim 新檔是獨立 footage，`parent_id` 指向來源。

---

## 4. 開程式時發生什麼

1. 讀 preference。
2. 檢查每個已知倉庫目錄在不在。
3. 目錄在：讀裡面的資料庫，核對檔案有沒有改名、刪掉、內容變了。
4. 目錄不在：這個倉庫離線，搜尋結果不要出現它的片子。
5. 還沒加入過的資料夾，可拖進 RollTag，或在設定裡新增路徑，登記成新倉庫再分析。
6. 監聽磁碟掛載／卸載；硬碟插回就重讀該倉庫。

加入或重掃大目錄時，工作在背景跑，介面不可卡住。必須看到：

- 階段：掃描目錄／核對身份／讀長度、拍攝時間與 GPS／寫入
- 百分比與進度條
- 已處理數量（掃描時為已找到幾支）
- 正在處理的檔案路徑
- 工具列與左側該倉庫列同步顯示百分比與檔名
- 已認得的片子可先出現在格線，不必等整批結束；縮圖等格子出現才做

---

## 5. 認檔

要快。第一輪只比相對路徑、檔名、大小、修改時間。內容比對只在對不上或 size／mtime 變了時才做。抽樣 hash，不是每次開程式把整顆硬碟重讀一遍。

身份用「路徑 + 檔案內容」一起認，不只靠檔名。

| 情況 | 反應 |
|---|---|
| 檔名改了，內容沒變 | 對回同一筆，改相對路徑，tags 留下 |
| 檔案被刪或搬走 | 標成找不到，一般搜尋不出現；「找不到」列表可看路徑、時間、GPS、標籤、雜湊等資料。可打開它所在目錄，或從資料庫刪掉單筆／全刪（只刪紀錄，檔本來就不在）。同內容回來就恢復 |
| 內容被改 | 當新內容：重做縮圖與 pHash；使用者打過的 tags 留下；拍攝時間與 GPS 依檔頭／DJI 檔名／檔案日期重讀 |
| 新檔出現 | 納入分析，等使用者打標 |
| 同一個內容出現在多個路徑 | 不自動合併。列成重複；選留下哪一支後其他進垃圾桶，或全部留下 |

---

## 6. 重複檔

重複有時是刻意的（同一段畫面放在不同旅行或備份），有時是無意複製。

- 重複組在掃描時就用 `content_hash` 寫進倉庫資料庫。點「重複檔」只讀這些組，不重算 hash、不掃整層目錄的原檔。有重點目錄時只留下至少有一支落在那些路徑的組
- 左側「重複檔」旁顯示尚未處理的組數（還沒選留下哪一支或全部留下）。處理完該組數字會減；有重點目錄時只算那些組。沒有未處理的組就不顯示數字
- 點左側「重複檔」時，右半改成比較頁：上半並排看出處目錄與畫面；縮圖先讀 `.rolltag/thumbs/`，沒有就只對目前這一組從原檔做並顯示。照片與影片都依轉正後比例，不鎖 16:9。原檔不在則刪縮圖。每支可獨立播（影片預設靜音），下半一組一組處理
- 能單組處理，也能一次把所有組都全部留下
- 每組選項：
  - 留下這一個：先確認。點上半要留的那一支，或按 A 留最左、D 留最右；tags／備註合併到留下的那支；Enter 確認後其他複本丟進垃圾桶，並從倉庫拿掉。只改這幾筆與資料庫列，不重掃整倉、不重算 hash
  - 全部留下：按 S 或不刪檔，當成不同檔，各自打標、各自出現在搜尋。一次寫完，不每組重載整庫

---

## 7. 單支與批次

凡是能批次做的事，一定也有單支做。手動打標與 AI 打標都一樣。  
Trim 只在單支。

可先圈幾個重點子目錄再打標或處理重複檔：點資料夾列進範圍；再勾旁邊的圈可一次圈多個。之後點「未打標」或「重複檔」不會丟掉這些目錄。範圍內全選（⌘A）再手打或 ⌥⌘T；或在「未打標」按右鍵直接批次 AI 打標。重複檔只列出至少有一支落在這些目錄的組，比較時仍看得到組內所有複本（可能在範圍外）。清掉重點後回到整倉。範圍只活在這次使用，不寫進設定。

---

## 8. 標籤

點開一支或一堆時，可打標。寫進該倉庫資料庫。

手打：格線選一支或一堆 → 右側點預設分類細項，或在輸入框打自訂字後按 Return／加入（逗號、頓號可一次多個）→ 點 chip 可拿掉。

AI 打標要先有 Gemini 或 OpenAI 的 API key（設定 → AI）：

- Gemini：用 Google 帳號到 [Google AI Studio API keys](https://aistudio.google.com/api-keys) 建立，貼進設定。說明見 [Using Gemini API keys](https://ai.google.dev/gemini-api/docs/api-key)
- OpenAI：到 [OpenAI API keys](https://platform.openai.com/api-keys) 建立 secret key，貼進設定。帳號可能要先開計費

用量由各平台計費。Key 只留在本機 `~/rolltag/config.json`。設定 → AI 也放這兩個連結。

選影片或照片後，按右側「AI 打標」或 ⌥⌘T（打完要完成／取消）；或側欄「未打標」／媒體區右鍵「AI 批次打標」（不必確認）。已打過也可再打。

**AI 打的標可能錯誤**（人數、地點、情緒、關鍵字都可能不對）。請自己看過再留下；不對的點掉。檢查器／⌥⌘T 會等確認；右鍵批次打完就寫入，更要事後檢查。

### 8.1 預設分類

大項先顯示，點進大項再出細項。分類在 App bundle 的 `DefaultTags.json`。  
舊 id（例如 `mood/calm`）保留，已打過的片子不會掉標。

主題（對齊 Pond5／Getty 瀏覽）：自然、城市、生活、旅行、飲食、商務、運動休閒、交通、動物、抽象空鏡。  
感覺與拍攝（對齊 Getty facet）：情緒、風格色調、天氣、地點、時間、鏡頭、人物。人物含人數：無人、一人、兩人、一群、人群。  
不做 Getty 族裔分類；人名用自訂 tag。

已打標的片子會進智慧列表「已打標」，並依它身上已有的大項出現在左側「分類」（例如打過情緒的會出現在「情緒」；自訂詞出現在「自訂」）。沒打過任何 tag 的只在「未打標」。一支可同時屬於多個分類。

### 8.2 自訂標籤

使用者可打自己的字，例如 `clubmed`。

- 輸入框加入；Return 或「加入」
- 逗號／頓號可一次加多個（`clubmed，小美`）
- 單支與批次同一套
- 用過的自訂標籤列出來，之後點一下可再加或移除
- 用過的自訂與預設分類中間有一小段分隔；預設區塊標「預設標籤分類」，以免看成自己定義過的標
- 已加的 tag、用過的自訂、預設大項與細項都換行排滿，右側資料欄往下捲就能全部看到，不橫向裁切
- 存在倉庫 DB，category 為 `custom`，值為使用者原文
- 搜尋自訂標籤的字要找得到
- 打非英語標（中文、日文等，或 App 不是英文）時，一併寫上英文關鍵字，格式跟 Getty／Pond5 一樣：小寫、詞與詞空白（`golden hour`、`sweden`、`chair`）。先對預設分類，再對設定裡的字詞對應表，再查國家／語言名與中英詞庫（CC-CEDICT，約十五萬詞，例如「椅子」→ `chair`、「漢堡」→ `hamburger`），對不上才用羅馬拼音。中英對照放資料檔，不寫在程式裡。複合詞會盡量對上字典裡最長的詞。對應表雙向：打其中一邊也會加上另一邊。點預設中文細項時也會加上對應英文詞。可點 chip 各別拿掉

點已加上的 chip 可移除。

### 8.3 手動 AI 打標

使用者自己按，匯入不跑。單支與批次都有。只打**影片與照片**。已打過 AI 或手打標的影片／照片仍可再打：會補新標，不拿掉手打或路徑標（同一顆以手打／路徑優先）。模型看的是抽幀與檔案脈絡，**建議可能錯**，不能當已核對。

1. 選取裡的音訊、小於 8 KB 的影片、以及抽不出幀／解不開的檔會跳過，狀態列寫跳過幾支；其餘照打
2. App 抽出約 6 幀 JPEG（約 768px；照片抽一張靜態圖）
3. Sidecar 先送使用中的平台（沒選則 Gemini 3.5 Flash Lite）；沒 key、配額或失敗立刻改送下一把有 key 的（通常是 gpt-4.1-mini），不等配額恢復。Gemini 只送最多 3 幀；OpenAI 送全部抽到的幀。批次一支接著一支，中間不停。
4. Prompt 附上預設分類（含中文名與英文名）＋目前已有的自訂 tag；預設分類 id 不在清單裡就丟掉。人物必須從無人／一人／兩人／一群／人群擇一，看不清當無人；沒人時不打人像／人群，也不寫 people 類英文關鍵字。模型另回 Getty／Pond5 英文關鍵字（小寫、空白分詞，例如 `icebreaker`、`arctic ocean`、`no people`），以及畫面裡看得清楚的短中文自訂詞。若 preference 裡有最近的打標範例，一併附上（AI 打了什麼、使用者留下什麼）；畫面仍是主要依據，範例只在相似畫面時當用詞習慣
5. 同時附上倉庫裡已有的檔案脈絡（缺的才現讀）：檔名、相對路徑／目錄、倉庫名、長度、檔案大小、拍攝時間、GPS。沒有的欄位不送。畫面仍是主要依據
6. **這支模型成功之後**，App 才寫標：先套路徑繼承，再加模型回傳的畫面標與英文關鍵字。中文自訂標再補字典／對應表／羅馬拼音；模型給的英文關鍵字直接當自訂標，不走拼音。模型／sidecar 失敗則都不寫
7. 路徑繼承只套**這個倉庫**已經出現過的自訂標，以及預設「地點」的中文名／英文名／id。比對前去掉空白與 `-_./`、忽略大小寫；拉丁字母只要是該層資料夾名的連續子字串（`clubmed` 對得上 `Club Med Ria`）；漢字必須與該層資料夾名完全相同（`馬` 不會套上 `馬來西亞`）。`source` 為 `path`
8. 模型打上的 tag `source` 為 `ai`。畫面上同一對 category／value 只顯示一次（手打優先於路徑，路徑優先於 AI），點掉就移除。第一版沒有單獨「清路徑標」
9. 不依拍攝小時自動打早晨／午後／夜晚
10. 從檢查器或 ⌥⌘T 打、且至少成功一支後，右側出現「完成」與「取消」。按完成或 Enter 保留這次標，並把這批「AI 打了什麼／留下什麼」寫進 preference（最多 8 則，優先記有改過的；不含畫面）。按取消或 Esc 拿掉這次打上的標（含同時寫入的路徑標），不記範例。沒按完不能再跑下一批 AI。全螢幕時 Esc 仍是先離開全螢幕。焦點在輸入欄時 Enter 仍是送出自訂標，Esc 不取消
11. 側欄「未打標」按右鍵「AI 批次打標」：目前重點目錄範圍內、可打的未打標影片／照片，一支一支打。媒體區對單一或多個選取按右鍵同一選單：點在已選的格子上就打目前選取裡可打的；點在未選的格子上只打那一支。這兩條路打完就留下，不出現完成／取消、不記範例
12. 批次一支一支送、中間不停。Gemini 免費／低額度比 OpenAI 更容易 429；失敗就立刻改送 OpenAI（有 key 時），不再空等。兩邊都失敗才算這支失敗。檔抽得出幀仍算失敗，不是跳過。狀態列寫成功與失敗支數。已成功的留下；失敗的可稍後再打

沒有 Gemini／OpenAI key 時，檢查器、選單與右鍵都不出現 AI 打標。選取裡沒有可打的影片／照片時，檢查器與 ⌥⌘T 不出現；已打過標的影片／照片仍算可打。key 怎麼取得見本章開頭。

### 8.4 拍攝時間與 GPS

掃描與 ⌘R 核對時重讀，用更好來源覆寫倉庫裡的拷貝日。

時間順位：EXIF／QuickTime 檔頭 → 檔名 `DJI_` 後 14 位 `YYYYMMDDHHMMSS` → 檔案系統建立／修改時間。只認 DJI 這種檔名，不從其他檔名猜時間。

檔頭早於 2015、晚於明天、或缺成年，預設略過、改下一源。設定 → AI 頁可改成「有檔頭就用檔頭」。檔名時間不當成有時區：檢查器顯示相機鐘，送給 AI 也不寫 `Z`。有帶時區的 EXIF／QuickTime 才用它的時區。

GPS 有才寫進倉庫。檢查器顯示座標數字（例如 `1.3700°, 103.8600°`；有高度再加海拔）。不開地圖、不反查地名。時間旁加一行來源：檔頭／DJI 檔名／檔案日期。

---

## 9. 切段

只選單一**影片**時，右側資料欄出現「剪輯」。按了才另開剪輯視窗：

- 手動 trim 出入點
- 存成倉庫內新檔（`.rolltag/trimmed/`）
- 新檔是獨立素材，自己打 tags，寫進同一個倉庫資料庫
- `parent_id` 指向來源
- 不做剪輯時間軸、調色、多軌（播放器進度條不算）
- 主視窗不內嵌 trim 控制

---

## 10. 搜尋與預覽

- 搜尋欄在下半媒體庫上緣：自然語言當關鍵字（空白、逗號斷詞）。點了或勾了倉庫裡的資料夾時，搜尋與格線只看這些路徑與其下層；搜尋欄提示資料夾名
- 搜尋欄右側有排序按鈕，決定格線順序：相關度、檔名、拍攝時間、修改時間、長度、檔案大小、類型；可選由小到大或由大到小
- 只從現在讀得到的倉庫找。離線硬碟、找不到的檔不出現在一般搜尋。點「找不到」可看這些檔的資料並刪紀錄或打開目錄
- 對得上的素材用 thumbnail 列在搜尋欄下方（照片捲到才讀內嵌小圖，依比例完整顯示）
- 搜尋相關度：tag 精確 > 自訂標籤部分符合 > 大項 > 檔名／路徑 > 備註 > 弱 pHash。選其他排序時只影響格線順序，不改誰出現。打非英語詞時一併用對應英文關鍵字去找：先預設細項，再設定裡的字詞對應表，再國家／語言名與中英詞庫（CC-CEDICT），對不上才羅馬拼音；例如「海」也會找到 `ocean`，「椅子」也會找到 `chair`。對應表雙向：搜英文關鍵字也會找到只打了對應常用詞的片子
- 時間、地點走 tag 與影片 metadata（有就用）。拍攝時間可排序；檢查器顯示時間來源與 GPS 座標。不依座標反查地名，不自動打時段標
- 畫面／風格／情緒的 CLIP 相似下一階段再接同一套 ranking
- 點選後格線立刻反白；上半左側接著播放（影片／音訊可拉時間軸跳轉）、右側看目錄與打標。單支影片按「剪輯」才開 trim 視窗
- 格線：影片／音訊 hover 可播放／暫停；照片只顯示靜態圖；點選、⌘／⇧ 多選。焦點在格線時，方向鍵或 W／A／S／D 移到畫面上上下左右相鄰那一格，並捲到看得見；⇧ 加選範圍
- 空白鍵控制上半播放器；P／Esc 進出全螢幕（預設；可在設定改）。全螢幕時 [ 或 , 上一則、] 或 . 下一則。重複檔對選中的那一支按全螢幕鍵審視，再按同一鍵或 Esc 回到比較頁

智慧列表：全部、已打標、未打標、找不到、重複。  
左側另有「分類」：只列出目前在線倉庫裡，至少有一支已打該大項的分類。點進去只看該大項的片子。  
左側倉庫可展開：目錄樹來自已索引素材的相對路徑。點倉庫看整倉；點某一層或勾幾個重點目錄，未打標／已打標／重複檔／搜尋都只先看那些資料夾與其下層。以 `.` 開頭的目錄（如 `.rolltag`）不列在樹上，仍可從整倉或智慧列表找到。

---

## 11. Sidecar

程式：`sidecar/rolltag_sidecar`。App 啟動時開、結束時關（關閉最後視窗或 ⌘Q）。本機 HTTP（`127.0.0.1`）。不做介面。結束後不留 Python 行程。

第一版：

- 抽樣 content hash（認檔／重複；App 內建 hasher 也可做，大目錄掃檔走背景）
- 代表幀 pHash、縮圖：掃描不做。格子出現時才抽一幀／讀內嵌小圖，並寫入 thumbs。
- `GET /health`
- `POST /hash`
- `POST /suggest_tags`：收 JPEG 幀（base64）、tag 清單（含英文名）、平台／key／模型、可選的最近打標範例；回 tags 與 Getty／Pond5 英文 keywords；只接 `gemini`、`openai`
- `POST /embed`：回 `not_enabled`

不做 CLIP、不做自動 AI 打標。抽幀由 App 用 AVFoundation 做，sidecar 只轉呼叫。

---

## 12. 資訊架構

- 左側：智慧列表旁顯示目前範圍內的數量（全部／已打標／未打標／找不到為檔數，線上且找得到的才算進全部與打標；找不到不需在線。「重複檔」為尚未處理組數）。已使用的 tag 分類、倉庫列表（離線變淡；掃描中顯示百分比與檔名；可展開其下目錄樹並勾重點目錄）。這一欄仍是側邊欄，不改右半佈局。有重點目錄時，媒體庫上緣與重複檔畫面顯示範圍列；側邊欄數字也只算那些資料夾。沒有就不顯示數字
- 右半上半左側：播放器（影片／音訊播放與可拖時間軸，照片顯示大圖）
- 右半上半右側：檔案資料（含拍攝時間與來源、有就顯示的 GPS）、tags（預設 + 自訂）、備註、單支影片的「剪輯」、有 key 且已選可打的影片或照片（含已打過標）時的 AI 打標
- 上半左右欄緊貼，中間不留空隙；資料欄固定寬
- 右半貼齊視窗左右，不留兩側空白
- 右半下半上緣：搜尋欄＋排序
- 右半下半：thumbnail 格線，貼齊左右、格子隨寬度撐滿
- 點左側「重複檔」：右半上半並排比較（目錄、倉庫、各自獨立播放），下半一組一組選留下哪一支或全部留下；預設 A 留最左、D 留最右、S 全留下，P 全螢幕看選中的那一支，Enter 確認丟垃圾桶（可在設定改）
- 設定視窗：倉庫路徑 CRUD、線上狀態；字詞對應表可增刪改常用詞與其英文；AI 分頁可捲動（使用中的平台、拍攝時間、API key 都在同一頁，視窗不夠高時從上面開始捲）；放 API key、使用中的平台、各平台能力足夠的模型選單、取得 Gemini／OpenAI key 的連結，以及檔頭時間是否略過不像拍攝日的值，並註明 AI 標可能錯；快捷鍵分頁依播放／媒體庫／重複檔改單鍵
- 重複檔視窗：同一套比較／分組畫面
- 剪輯視窗：單支影片 trim
- 快捷鍵一覽視窗：從說明選單或檢視選單打開，列出程式、媒體庫、播放、重複檔的按鍵（後三者跟設定裡目前的綁定走）
- 選單：File／Warehouse／Tag／Playback，加上系統 Edit／View／Help

---

## 13. 之後（不做進第一版）

- 搬移 warehouse：整個倉庫換位置時 metadata 一起搬
- 原生送整段影片（含聲音）給 Gemini／Twelve Labs／Qwen
- 倉庫丟進 NAS
- DaVinci 串接
- Linux／Windows 客戶端（另做，不改掉 Mac UI）

---

## 14. 變更紀錄

只記產品行為，不記重構細節。

| 日期 | 版本 | 變更 |
|---|---|---|
| 2026-09-19 | 1.0 | 第一版規格落地：倉庫、認檔、預覽、預設 tags、trim、重複檔、hybrid 搜尋 |
| 2026-09-19 | 1.0 | 加入大目錄時顯示階段、檔名、數量與百分比；掃描在背景跑 |
| 2026-09-19 | 1.0 | 使用者可打自訂 tag（例如 `clubmed`）；可搜尋、可再點用過的標籤 |
| 2026-09-19 | 1.0 | 預設 tags 對齊 Getty／Pond5：補主題桶與鏡頭／人物細項；已打標片子進「已打標」與左側分類 |
| 2026-09-19 | 1.0 | Preference 可存各 AI 平台 API key 與使用中平台；仍不自動打標 |
| 2026-09-19 | 1.0 | 手動抽幀打標：預設 Gemini 2.5 Flash Lite，其次 gpt-4.1-mini；匯入仍不自動跑 |
| 2026-09-19 | 1.0 | Preference 用選單挑能力足夠的模型；有 key 且已選影片才出現 AI 打標 |
| 2026-09-19 | 1.0 | 倉庫認 iPhone 照片（HEIC／JPEG／PNG／WebP）與 MP3 等音訊；影片抽幀改等檔案就緒，抽不到才標無法預覽 |
| 2026-09-19 | 1.0 | Gemini 預設改 `gemini-3.5-flash-lite`；舊的 2.5 Flash Lite 對新帳號已不可用 |
| 2026-09-19 | 1.0 | 預覽改走 Quick Look 快取並並行抽 4 支；格線可見的格子先出系統縮圖 |
| 2026-09-19 | 1.0 | 照片改 lazy：掃描只讀 EXIF，格子出現才用內嵌小圖顯示 |
| 2026-09-19 | 1.0 | 特殊比例照片完整顯示、不溢出；點選後右側可看大圖 |
| 2026-09-19 | 1.0 | 照片縮圖改 lazy：第一次顯示前才建立並寫入 thumbs |
| 2026-09-19 | 1.0 | 掃描不再批次解影片預覽；縮圖改單支 lazy，避免整台卡死 |
| 2026-09-19 | 1.0 | 主視窗改左欄＋右半：上搜尋、上半播放與資料／打標、下半格線；剪輯改獨立視窗；空白鍵播放、P／Esc 全螢幕 |
| 2026-09-19 | 1.0 | 右半上半左右欄與格線改緊貼，不留左右空白 |
| 2026-09-19 | 1.0 | 右半內容撐滿，去掉播放器與格線兩側空白 |
| 2026-09-19 | 1.0 | 搜尋欄移到媒體庫上緣，右側可選格線排序 |
| 2026-09-19 | 1.0 | 掃描中不重算整格、不抽新縮圖，避免 CPU／記憶體被進度刷新拖高 |
| 2026-09-19 | 1.0 | 右側標籤改換行排滿，可全部看到 |
| 2026-09-19 | 1.0 | 重複檔分頁改上半比較目錄與畫面、下半一組一組選留下或全留 |
| 2026-09-19 | 1.0 | 重複檔比較區每支可獨立播放 |
| 2026-09-19 | 1.0 | 選「留下這一個」後，其他複本丟進垃圾桶並從倉庫移除 |
| 2026-09-19 | 1.0 | 重複檔可用 A 留最左、D 留最右、S 全留下，Enter 確認把其他複本丟進垃圾桶 |
| 2026-09-19 | 1.0 | 說明選單與檢視選單可打開快捷鍵一覽，列出目前所有按鍵 |
| 2026-09-19 | 1.0 | 格線不再把整庫縮圖解進記憶體；格子只留小圖，點選後的大圖從原檔抽 |
| 2026-09-19 | 1.0 | 同一顆標籤若使用者與 AI 都打過，畫面與用過的自訂標籤只顯示一次 |
| 2026-09-19 | 1.0 | 影片／音訊播放器可拉時間軸跳轉 |
| 2026-09-19 | 1.0 | 預覽失敗會說明是檔案不完整，或系統解不開 |
| 2026-09-20 | 1.0 | 手動 AI 打標會附上有的檔名、路徑、倉庫名、長度、檔案大小、拍攝時間與 GPS |
| 2026-09-20 | 1.0 | 拍攝時間順位：檔頭 → DJI 檔名 → 檔案日期；略過不像拍攝日的檔頭（可在設定改）；時間與 GPS 進倉庫並在檢查器顯示來源與座標 |
| 2026-09-20 | 1.0 | AI 只打影片與照片；批次跳過音訊／空檔／解不開。模型成功才套本倉庫已有的路徑地點／自訂標（`source=path`） |
| 2026-09-20 | 1.0 | 英文介面的預設標籤顯示英文名，跟著 App 語言而不是系統地區 |
| 2026-09-20 | 1.0 | 定位：搜尋綁倉庫，換 DaVinci 案子不必重做素材庫索引（不做串接；對照 Super Bin） |
| 2026-09-21 | 1.0 | 左側可展開倉庫目錄樹；點一層資料夾後搜尋與格線只看該路徑與其下層 |
| 2026-09-21 | 1.0 | 可勾選多個重點子目錄；未打標、搜尋、AI 打標與重複檔只先處理那些目錄 |
| 2026-09-21 | 1.0 | 重複檔畫面用資料庫 content hash 組，不再每次掃整倉或解原圖；比較縮圖先讀已存 thumbs |
| 2026-09-21 | 1.0 | 重複檔沒有現成縮圖時當場從原檔做；原檔不在就刪掉對應 thumbs |
| 2026-09-21 | 1.0 | 處理重複檔只改該組與刪掉的檔，不重掃倉庫；進行中的對帳不會把剛刪的列寫回 |
| 2026-09-21 | 1.0 | 左側「重複檔」顯示尚未處理的組數；有重點目錄時只算那些組 |
| 2026-09-21 | 1.0 | 左側全部／已打標／未打標／找不到也顯示目前範圍內的檔數 |
| 2026-09-21 | 1.0 | 直立照片依 EXIF 轉正並按比例顯示；重複檔比較不再鎖 16:9；比例不對的舊縮圖會重做 |
| 2026-09-21 | 1.0 | 直立影片依軌道方向轉正，格線與重複檔比較不再鎖 16:9；比例不對的舊海報會重做 |
| 2026-09-21 | 1.0 | 注音等輸入法打出怪字元時提示切到英文；重複檔對選中的檔按 P 全螢幕，再按 P 或 Esc 回來 |
| 2026-09-21 | 1.0 | 「找不到」可看路徑與 metadata、打開目錄，或從資料庫刪單筆／全刪 |
| 2026-09-21 | 1.0 | 非英語標同時寫 Getty／Pond5 風格英文關鍵字；AI 打完要按完成或 Enter 確認 |
| 2026-09-21 | 1.0 | AI 打完可按取消或 Esc 拿掉這次標；完成／Enter 才保留 |
| 2026-09-21 | 1.0 | 搜尋打非英語詞時一併對預設細項英文名／id 或羅馬拼音，例如「海」也會找到 ocean |
| 2026-09-21 | 1.0 | 焦點在媒體庫格線時，方向鍵移到畫面上相鄰的素材；⇧ 加選 |
| 2026-09-21 | 1.0 | 設定新增快捷鍵分頁：播放、媒體庫、重複檔的單鍵可改，寫進 ~/rolltag/config.json |
| 2026-09-21 | 1.0 | 媒體庫格線 W／A／S／D 也等同上下左右；若該鍵已改成別的動作就不再當方向 |
| 2026-09-21 | 1.0 | 非英語打標與搜尋先對預設分類與國家／語言／地名字典（瑞典→sweden），對不上才羅馬拼音 |
| 2026-09-21 | 1.0 | 常用詞也走字典（破冰船→icebreaker），複合詞對最長詞，不再誤用羅馬拼音 |
| 2026-09-21 | 1.0 | 設定新增字詞對應表；打標與搜尋雙向套用，優先於字典與羅馬拼音 |
| 2026-09-21 | 1.0 | 全螢幕時 [ 或 , 上一則、] 或 . 下一則；依目前清單順序，找不到的檔跳過 |
| 2026-09-21 | 1.0 | AI 打標除清單與中文標外，另寫 Getty／Pond5 英文關鍵字；畫面裡看得清楚的短中文自訂詞可留下 |
| 2026-09-21 | 1.0 | 中英字典改載 CC-CEDICT 詞庫（日常詞如椅子、森林、漢堡），對照表在資料檔不在程式裡 |
| 2026-09-21 | 1.0 | 右側用過的自訂標籤與預設分類中間加分隔，並標「預設標籤分類」 |
| 2026-09-21 | 1.0 | AI 打標按完成時記下最近「AI 打了什麼／留下什麼」，下次當短範例送出；取消不記 |
| 2026-09-22 | 1.0 | AI 打標必須從無人／一人／兩人／一群／人群擇一；看不清當無人，沒人就不寫 people 類關鍵字 |
| 2026-09-22 | 1.0 | 已打過標的影片／照片仍可再按 AI 打標；側欄未打標與媒體區右鍵可批次打、不必確認 |
| 2026-09-22 | 1.0 | 說明手打與 AI 打標步驟、Gemini／OpenAI API key 怎麼取得；並註明 AI 打的標可能錯誤 |
| 2026-09-22 | 1.0 | 說明大批次 AI 打標可能因平台配額、頻率上限或逾時失敗，檔案本身往往沒問題 |
| 2026-09-22 | 1.0 | 設定 AI 分頁可捲動，使用中的平台與拍攝時間不再被裁在視窗外 |
| 2026-09-22 | 1.0 | 關閉最後一個視窗或 ⌘Q 即結束；Python sidecar 一併關掉，不留在行程裡 |
| 2026-09-22 | 1.0 | 開過設定後空白鍵／P 等播放快捷鍵仍可用；只有設定視窗在最前時才不攔截 |
| 2026-09-22 | 1.0 | Gemini 批次間隔與 429 重試後再備援 OpenAI；只送 3 幀，降低比 OpenAI 高的失敗率 |
| 2026-09-22 | 1.0 | 拿掉批次空等與長重試；Gemini 失敗立刻改送 OpenAI，避免整批變很慢 |
| 2026-09-22 | 1.0 | 點格線立刻反白與換資料欄；大圖先用縮圖、影片在背景換檔，不再等 1–3 秒才有反應 |
