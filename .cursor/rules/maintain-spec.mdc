---
description: 行為一變就更新 SPEC.md，保持規格書與實作同步
alwaysApply: true
---

# 持續維護規格書

[`SPEC.md`](SPEC.md) 是 RollTag 的產品規格與現況。改程式時同一輪一起改它，不要等之後補。

必須更新規格的情況：

- 使用者看得到或做得到的行為變了（掃描、認檔、打標、搜尋、trim、重複檔、進度、離線）
- 資料放哪變了（`~/rolltag/config.json`、倉庫 `.rolltag/`、schema、預留欄）
- 第一版範圍變了：新功能從「不做／預留」搬進「做」；或相反
- 快捷鍵、支援的副檔名、sidecar API 變了

怎麼改：

- 改對應章節，讓內文描述**現在**的行為，不要留過期句子
- 在「變更紀錄」加一列：日期、版本、為什麼變（寫行為，不寫檔名清單）
- 文件開頭的產品版本與日期一併改
- 預留或明確不做的項目，維持寫在對應節，不要刪掉以免後面接錯

不要：

- 只改程式、規格還寫舊規則
- 把實作細節（型別、函式名）塞進規格，除非它就是產品契約（路徑、API、認檔表）
- 另寫一份互相打架的說明；README 只做開專案與資料位置摘要，細節以 SPEC 為準
